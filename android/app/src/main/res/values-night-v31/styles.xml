<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Android 12+ SplashScreen API：用 vector splash_icon.xml 作为 icon。
         vector 在任意密度下都是清晰的；icon 在 108dp viewport 里只占
         中间 ~40dp，整体不会被放大模糊。Flutter `_SplashScreen` 紧接
         用同样的蓝底 + 小 AppLogo 接管，视觉上连续。 -->
    <style name="LaunchTheme" parent="Theme.SplashScreen">
        <item name="postSplashScreenTheme">@style/NormalTheme</item>
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="android:windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="android:forceDarkAllowed">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
    <style name="NormalTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <!-- 锁定为品牌色，避免 Flutter 第一帧渲染前的白屏闪烁 -->
        <item name="android:windowBackground">@color/splash_background</item>
        <!-- 显示系统状态栏，由 Flutter AnnotatedRegion 控制图标颜色 -->
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <!-- 深色主题：白色状态栏图标 -->
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>
